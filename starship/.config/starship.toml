format = """
$directory\
$git_branch\
$git_status\
$terraform\
$kubernetes\
$character"""

#[fill]
#symbol = ' '

[terraform]
format = '[TF $workspace]($style) '


[git_status]
conflicted = 'conflict'
ahead = 'ahead'
behind = 'behind'
diverged = 'diverged'
#up_to_date = '✓'
untracked = '*'
stashed = ''
modified = '*'
staged = '[++\($count\)](green)'
renamed = 'renamed'
deleted = 'deleted'

[kubernetes]
disabled = true
